var React = require('react');

var Countdown = React.createClass({
  render: function () {
    return (
      <div>
        <p>"Countdown.jsx Active. or whatever. u no wat i sain?"</p>
      </div>
    )}
});

module.exports = Countdown;
