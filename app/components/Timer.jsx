var React = require('react');

var Timer = React.createClass({
  render: function () {
    return (
      <div>
        <p>"Timer.jsx Active. or whatever. u no wat i sain?"</p>
      </div>
  )}
});

module.exports = Timer;
